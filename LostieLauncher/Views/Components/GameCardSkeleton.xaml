<UserControl x:Class="LostieLauncher.Views.Components.GameCardSkeleton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Name="Root">
    <!-- Mirrors the outer geometry of GameCardComponent: Grid[Margin=0,0,0,10] > Border[SecondaryBg, r6, p16] -->
    <Grid Margin="0,0,0,10">
        <Border Background="{DynamicResource SecondaryBgBrush}" CornerRadius="6" Padding="16" SnapsToDevicePixels="True">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!-- Logo placeholder: 110×80, r6 — mirrors the logo Border in GameCardComponent -->
                <Border Grid.Column="0"
                        Style="{StaticResource SkeletonBlockStyle}"
                        Width="110" Height="80" CornerRadius="6"
                        VerticalAlignment="Center" />

                <!-- Text placeholder: title bar + meta bar — mirrors the StackPanel text column -->
                <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="16,0,16,0">
                    <Border Style="{StaticResource SkeletonBlockStyle}"
                            Height="18" CornerRadius="3"
                            Width="180" HorizontalAlignment="Left" />
                    <Border Style="{StaticResource SkeletonBlockStyle}"
                            Height="12" CornerRadius="3"
                            Width="120" HorizontalAlignment="Left"
                            Margin="0,8,0,0" />
                </StackPanel>

                <!-- Button placeholder: 110×32, r4 — mirrors CardAccentButtonStyle dimensions -->
                <Border Grid.Column="2"
                        Style="{StaticResource SkeletonBlockStyle}"
                        Width="110" Height="32" CornerRadius="4"
                        VerticalAlignment="Center" />
            </Grid>
        </Border>
    </Grid>
</UserControl>

<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        The launcher is a client of the project CDN: the game catalogue, the
        home content and the maintenance flag all come over HTTPS. Cleartext is
        not enabled, so `usesCleartextTraffic` stays at its API 28+ default of
        false and every endpoint in `ContentOptions` must be https://.
    -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".LostieLauncherApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LostieLauncher">

        <!-- Label and theme are inherited from <application> above. -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>

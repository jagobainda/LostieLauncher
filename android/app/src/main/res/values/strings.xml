<?xml version="1.0" encoding="utf-8"?>
<!--
    The launcher's user-visible text is not localized through this file. The
    desktop side keeps its eight languages in code (Content/Strings.cs) and
    switches them at runtime without restarting; port plan step 05 ports that
    catalogue and the mechanism. Only the values Android itself reads before any
    Kotlin runs — the launcher label — belong here.
-->
<resources>
    <string name="app_name">Lostie Launcher</string>
</resources>
